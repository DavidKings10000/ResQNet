export async function GET() {
  return Response.json({ message: 'Auth routes are handled by the ResQNet custom JWT flow.' }, { status: 200 });
}

export async function POST() {
  return Response.json({ message: 'Auth routes are handled by the ResQNet custom JWT flow.' }, { status: 200 });
}
